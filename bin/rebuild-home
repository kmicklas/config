#!/bin/sh
BASEDIR=$(dirname "$0")
home-manager switch \
  -I nixpkgs=$(nix eval --raw "(import $BASEDIR/../dep/nixpkgs/thunk.nix)") \
  -I home-manager=$(nix eval --raw "(import $BASEDIR/../dep/home-manager/thunk.nix)") \
  $@
